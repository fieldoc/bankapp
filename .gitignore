# SQLite databases (real financial data — never commit)
*.db
*.db-journal
*.db-wal

# Ingest / working folders
inbox/
exports/
quarantine/

# Secrets / env
.env

# Build-process progress board (transient; removed by /full:save)
FULL-BUILD-PLAN.md

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# macOS app build artifacts
scripts/mac-app/build/
scripts/mac-app/icon_1024.png
FULL-BUILD-PLAN.md
