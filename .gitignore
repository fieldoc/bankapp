# SQLite databases (real financial data — never commit)
*.db
*.db-journal
*.db-wal

# Ingest / working folders
inbox/
exports/
quarantine/

# Secrets / env
.env

# Build-process progress board (transient; removed by /full:save)
FULL-BUILD-PLAN.md

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# macOS app build artifacts (the icon-*.png masters ARE committed — see scripts/mac-app/build.sh)
scripts/mac-app/build/
FULL-BUILD-PLAN.md
